# 正则表达式
+----------+----------------------------+
| 正则     | 说明                       |
+----------+----------------------------+
| ^word    | 该行以word开头             |
+----------+----------------------------+
| word$    | 该行以word结尾             |
+----------+----------------------------+
| ^$       | 表示空行                   |
+----------+----------------------------+
| .        | 代表一定有一个任意字符     |
+----------+----------------------------+
| *        | 前面的字符存在零个或无穷个 |
+----------+----------------------------+
| \        | 转移字符                   |
+----------+----------------------------+
| [chars]  | 匹配chars中的一个字符      |
+----------+----------------------------+
| [n-m]    | 指定一个序列               |
+----------+----------------------------+
| [^chars] | 反向匹配chars中的字符      |
+----------+----------------------------+
| \{n\}    | 前面的字符存在n个          |
+----------+----------------------------+
| \{n,\}   | 前面的字符至少存在n个      |
+----------+----------------------------+
| \{n,m\}  | 前面的字符存在n至m个       |
+----------+----------------------------+

# 扩展正则表达式
+----------+--------------------------+
| 扩展正则 | 说明                     |
+----------+--------------------------+
| +        | 前一个字符存在一个或多个 |
+----------+--------------------------+
| ?        | 前一个字符存在零个或一个 |
+----------+--------------------------+
| |        | 或关系                   |
+----------+--------------------------+
| ()       | 群组字符串               |
+----------+--------------------------+

# 不要空行
# 不要井号开头的行
+-------------------------------------------------+
| grep -v '^$' /etc/manpath.config | grep -v '^#' |
+-------------------------------------------------+

# 不要空行
# 不要井号开头的行
# 使用扩展正则表达式
+--------------------------------------+
| grep -vE '^$|^#' /etc/manpath.config |
+--------------------------------------+
| egrep -v '^$|^#' /etc/manpath.config |
+--------------------------------------+

# 查找g和d之间存在一个或多个o的字符串
+-------------------------------------+
| egrep -n 'go+d' regular_express.txt |
+-------------------------------------+

# 查找g和d之间存在一个o或不存在o的字符串
+-------------------------------------+
| egrep -n 'go?d' regular_express.txt |
+-------------------------------------+

# 查找gd或good
+----------------------------------------+
| egrep -n 'gd|good' regular_express.txt |
+----------------------------------------+

# 查找glad和good
+------------------------------------------+
| egrep -n 'glad|good' regular_express.txt |
+------------------------------------------+
| egrep -n 'g(la|oo)d' regular_express.txt |
+------------------------------------------+

# 查找AxyzxyzxyzxyzxyzC
+---------------------+
| egrep -n 'A(xyz)+C' |
+---------------------+

